<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity"
    android:background="@color/blue">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:textSize="35dp"
        android:text="Set Timer"
        android:id="@+id/setTimerReminder"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:textColor="@color/nigga"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/timeOkayButton"
        android:id="@+id/timeOkayButton"
        android:onClick="timeSelected"
        android:textColor="@color/nigga"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" />

    <Spinner
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/timeSpinner"
        android:spinnerMode="dropdown"
        android:textColor="@color/white"
        android:layout_below="@+id/chooseNumberMinutes"
        android:layout_alignLeft="@+id/chooseNumberMinutes"
        android:layout_alignStart="@+id/chooseNumberMinutes" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:textSize="25dp"
        android:text="@string/chooseNumberMinutes"
        android:id="@+id/chooseNumberMinutes"
        android:layout_below="@+id/setTimerReminder"
        android:layout_centerHorizontal="true"
        android:textColor="@color/nigga"/>
</RelativeLayout>
